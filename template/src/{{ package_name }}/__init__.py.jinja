"""Public package surface for **{{ project_name }}**.

{{ project_description }}

The version string is exposed as :data:`__version__` for use in packaging and runtime checks.
"""

from __future__ import annotations

from importlib.metadata import PackageNotFoundError, version

try:
    __version__ = version("{{ package_name }}")
except PackageNotFoundError:
    __version__ = "unknown"

__all__ = ["__version__"]
