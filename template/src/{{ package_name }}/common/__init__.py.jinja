"""Internal support utilities for **{{ project_name }}**.

Submodules provide optional cross-cutting helpers (logging, decorators, filesystem)
used by the public package. They are not considered stable API unless re-exported
from the top-level package.
"""
