# CLAUDE.md

**Shared knowledge lives in [AGENTS.md](./AGENTS.md) — read it first.**

## Claude-specific notes

- Role: implementation, tests, standards enforcement via `.claude/hooks/` and `just` recipes.
- Deeper detail: `.claude/references/` and `.claude/skills/` (load on demand per AGENTS.md).
- Memory: AgentMemory only (`{{ project_slug }}`). No MEMORY.md / CONFLICTS.md.

## Quick commands

| Task | Command |
|---|---|
| Tests | `just test` |
| Lint / format | `just lint` / `just fmt` |
| Type check | `just type` |
| Full CI | `just ci` |
{% if include_cli %}| CLI | `just run -- --help` |{% endif %}
